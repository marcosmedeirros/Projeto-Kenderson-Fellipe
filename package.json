{
  "name": "controladoria-do-canal",
  "private": true,
  "description": "Ferramentas de desenvolvimento (só para gerar o CSS). O painel roda em PHP.",
  "scripts": {
    "build:css": "tailwindcss -i resources/app.css -o assets/app.css --minify",
    "watch:css": "tailwindcss -i resources/app.css -o assets/app.css --watch",
    "dev": "php -S localhost:8080 dev-router.php"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
